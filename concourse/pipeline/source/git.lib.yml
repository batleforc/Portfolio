#@ def gitTemplate(repo_url,branch_name,label,path):
name: #@ "git" + label
type: git
icon: gitlab
source:
  uri: #@ repo_url
  branch: #@ branch_name
  private_key: ((ci.privatekey))
  paths:
    - #@ path
#@ end
