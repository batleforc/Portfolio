#@ load("@ytt:data", "data")
#@ def deployressource():
name: deploy-ressource
type: deploy
icon: kubernetes
source:
  url: ((ci.cluster_url))
  certificate_authority: ((ci.cluster_ca))
  namespace: #@ data.values.project.name
  token: ((ci.token))
#@ end
