#@ load("@ytt:data", "data")
#@ def discordRessources(path,app):
name: notif-discord
type: webhook-notification
icon: bell
source:
  webhook: #@ data.values.project.webhook
  sonarqube: #@ data.values.project.qube
  projectname: #@ data.values.project.name
  domain: #@ data.values.project.domain
  path: #@ path
  app: #@ app
#@ end
